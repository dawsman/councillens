# Python dependencies for the ingest, transform, and validation stages.
requests>=2.31
PyYAML>=6.0
jsonschema>=4.0
beautifulsoup4>=4.12
pypdf>=4.0

# Publish stage (static site rendering).
Jinja2>=3.1
markdown>=3.5
